<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="Explore delicious recipes including breakfast, lunch, dinner, and desserts.">
  <meta name="keywords" content="recipes, food, cooking, easy meals">
  <meta name="author" content="Simplicious">
  <title>Explore Recipes</title>  
  <link rel="icon" type="image/png" href="/images/panlog.png" />
  <link rel="stylesheet" href="css/recipeCSS.css" />
</head>
<body>
    
    <%- include('../partials/header') %>

  <section>
    <div class="carousel">
      <div class="carousel-images" id="carouselImages">
        <img src="/images/burger(recipe).jpg" alt="burger">
        <img src="/images/quesadeila(recipe).jpg" alt="quesadilla">
        <img src="/images/pasta(recipe).jpg" alt="pasta">
      </div>
      <div class="dots-container" id="dots">
        <span class="dot active"></span>
        <span class="dot"></span>
        <span class="dot"></span>
      </div>
    </div>

    <h2>Explore Recipes</h2>

    <div class="grid">
      <% categories.forEach(category => { %>
        <a href="/recipes/category/<%= category.name.toLowerCase() %>" class="nav-card">
          <img src="/images/<%= category.image %>" alt="<%= category.name %>">
          <button><%= category.name %></button>
        </a>
      <% }) %>
    </div>
  </section>

  <script src="/recipe.js"></script>

  <%- include('../partials/footer') %>

</body>
</html>
